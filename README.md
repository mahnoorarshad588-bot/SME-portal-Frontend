
  # Redesign homepage with icons (Copy)

  This is a code bundle for Redesign homepage with icons (Copy). The original project is available at https://www.figma.com/design/fujAkRiNazurPvl18HcWwS/Redesign-homepage-with-icons--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  